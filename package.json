{
  "name": "globalvbc",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "GlobalVBC — single Vercel project (SPA + Express API as a serverless function)",
  "scripts": {
    "build": "npm install --prefix frontend && npm run build --prefix frontend",
    "db:migrate": "node backend/migrate.js",
    "seed": "node backend/seed.js"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.0",
    "express-rate-limit": "^7.4.0",
    "helmet": "^8.0.0",
    "jsonwebtoken": "^9.0.2",
    "pg": "^8.13.0",
    "pino": "^9.5.0"
  }
}
